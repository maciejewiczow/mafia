{
    "navigationFallback": {
        "rewrite": "/index.html",
        "exclude": ["*.{css,scss,png,gif,ico,jpg,svg}"]
    },
    "routes": [
        {
            "route": "/api/TurnCallback",
            "allowedRoles": ["anonymous"]
        }
    ]
}
